{% extends "base.html.twig" %}

{% block title 'edit pin' %}

{% block body %}
    <div class="container">
    <h3>Edit Pin</h3>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Update</button>
    {{ form_end(form) }}  
    </div>
{% endblock %}
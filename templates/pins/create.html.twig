{% extends "base.html.twig" %}
{% block title 'New pin' %}

{% block body %}
    <div class="container">
    <h3>Create Pin</h3>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Save</button>
    {{ form_end(form) }}  
    </div>
{% endblock %}
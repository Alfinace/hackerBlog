{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Server",
            "type": "process",
            "command": "php -S localhost:8080 -t ${workspaceFolder}/public",
            "isBackground": true,
            "group": "build",
            "problemMatcher": []
        },
        {
            "label": "Run In Browser",
            "type": "process",
            "command": "open http://localhost:8080/${relativeFile}",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
